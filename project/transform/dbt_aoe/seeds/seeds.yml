version: 2

seeds:
  - name: country_list
    description: 'list of country codes and names for aoe2 players' 
    docs:
      show: true
      node_color: purple
    # config:
    #   schema: landing
    # tests:
    #   - <test>
    columns:
      - name: Country
        description: 'Name of country'
      - name: Country_code
        description: 'Country codes in lowercase'